// Inherit the parent event
event_inherited();
xstart = x;
ystart = y;

// Tipo de movimento ("circle", "horizontal", "vertical")
moveType = "circle";

// Configurações gerais
radius = 32;
rotSpd = 2;      // graus por step
dir = 0;

moveDist = 64;   // distância para linear
moveSpeed = 2;   // velocidade para linear